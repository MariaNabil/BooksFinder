import React from 'react';
import {View} from 'react-native';
import {mainColor} from '../../constants/colors';

export default () => {
  return (
    <View style={{backgroundColor: mainColor, height: 0.5, opacity: 0.3}} />
  );
};
